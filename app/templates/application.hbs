<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">
        <img alt="Brand" src="...">
      </a>
    </div>
    contact
    auth stuff
    account management
  </div>
</nav>

<div class="container-fluid">
  <div class="row">
    <div class="left-pane col-md-3">
      {{#each stock in pinnedStocks}}
        {{stock-pin stock=stock}}
      {{/each}}
    </div>
    <div class="main-pane col-md-9">
      {{#each signal in signals}}
        {{signal-notification signal=signal}}
      {{/each}}
    </div>
    {{outlet}}
  </div>
</div>
